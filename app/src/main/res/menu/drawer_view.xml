<?xml version="1.0" encoding="utf-8"?>
<menu
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:checkableBehavior="single">
        <item
                android:id="@+id/nav_home"
                android:icon="@drawable/ic_home_cherry"
                android:title="Home"
                android:checked="true"/>
        <item
                android:id="@+id/nav_profile"
                android:icon="@drawable/ic_person_cherry"
                android:title="Profile" />
        <item
                android:id="@+id/nav_loc_switcher"
                android:title="Location Service"
                android:icon="@drawable/ic_location_on_cherry"
                app:actionLayout="@layout/layout_switch"
        />
    </group>
</menu>